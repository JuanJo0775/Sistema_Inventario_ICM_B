services:
  test:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: test
    env_file:
      - .env
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.test
      SKIP_DB_WAIT: "1"
    volumes:
      - .:/app
    command: pytest
