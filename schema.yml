openapi: 3.0.3
info:
  title: ICM — API de Gestión de Inventario y Operaciones
  version: 1.0.0
  description: |-
    API REST del sistema **ICM (Import Corporal Medical)**.

    **Schema:** [/api/schema/](/api/schema/)

    ---
    ### **Guía de Autenticación**
    Para probar los endpoints, haga clic en el botón **Authorize**, use el esquema *BearerAuth* e ingrese el token con el prefijo literal:
    `Bearer <access_token>`

    > *Token obtenido vía `POST /api/v1/auth/login/`*

    ---
    ### **Control de Acceso (RBAC)**
    - **Roles:** Almacenista, Auxiliar de Despacho, Administrador.
    - **Restricción Horaria:** El rol `auxiliar_despacho` está sujeto a la regla **BR-03** (07:00-12:00 / 14:00-17:00).

    ---
    ### **Módulo de Precios y Facturación — Opcional / No bloqueante**

    > ⚠️ Este módulo es **completamente opcional**. El sistema funciona con normalidad sin que los productos tengan precios configurados.

    El sistema incluye soporte de precios como capa aditiva sobre el ledger de inventario (**RF-013**). Su integración con el frontend puede realizarse de forma incremental:

    **Lo que cambia en respuestas existentes (campos adicionales, siempre `null` por defecto):**
    - `GET /catalog/products/<id>/` → incluye `unit_cost`, `sale_price_retail`, `sale_price_wholesale`, `tax_rate_pct`, `currency` (null si no se configuraron)
    - `GET /movements/dispatches/` → incluye `unit_price`, `subtotal`, `tax_amount`, `total_amount`, `currency`, `price_type`, `customer_snapshot` (null si el producto no tiene precio)
    - `GET /catalog/combos/<id>/` → incluye `price_strategy` (`"derived"`), `fixed_price_retail`, `fixed_price_wholesale` (null)

    **Lo que NO cambió (backward-compatible al 100%):**
    - `POST /movements/dispatches/` — sin campos requeridos nuevos. El campo opcional `discount_pct` puede omitirse.
    - `POST /catalog/products/` — sin campos requeridos nuevos.
    - `PUT/PATCH /catalog/products/<id>/` — los campos de precio son ignorados aquí; solo se modifican vía `/prices/`.
    - `POST /catalog/combos/` — `price_strategy` es opcional, default `"derived"`.

    **Endpoints nuevos (el frontend los ignora hasta que los necesite):**
    - `PATCH /catalog/products/<id>/prices/` — configurar precios del producto (solo almacenista)
    - `GET  /catalog/products/<id>/prices/` — historial inmutable de cambios de precio
    - `GET  /movements/invoices/<number>/` — detalle de factura con totales comerciales
    - `GET  /movements/invoices/<number>/pdf/` — PDF enriquecido con precios
    - `GET  /reports/revenue-summary/` — revenue por tipo de venta
    - `GET  /reports/margin-by-product/` — margen bruto por SKU
    - `GET  /reports/sales-by-customer/` — ventas por cliente (venta mayor)

    ---
    ### **Documentación Adicional**
    - **API (referencia detallada):** `docs/api/README_API.md`
    - **Requisitos:** `docs/requisitos/ERS_ICM_Requisitos.md`
    - **Arquitectura:** `docs/README_ARQUITECTURA.md`
    - **Precios:** `docs/pricing/README_PRECIOS_FACTURACION.md`
    - **Nota:** el stock se maneja como un ledger inmutable con stock derivado.
  contact:
    name: ICM / Proyecto Nuclear 3 — Universidad Alexander von Humboldt
  license:
    name: Proyecto Formativo / Uso académico — Ley 1581
paths:
  /api/v1/alerts/:
    get:
      operationId: alerts_list
      description: Lista las alertas activas y permite exportarlas.
      summary: Listar alertas activas
      parameters:
      - in: query
        name: alert_type
        schema:
          type: string
      - in: query
        name: category
        schema:
          type: string
      - in: query
        name: date_from
        schema:
          type: string
        description: Fecha desde (YYYY-MM-DD).
      - in: query
        name: date_to
        schema:
          type: string
        description: Fecha hasta (YYYY-MM-DD).
      - in: query
        name: location_id
        schema:
          type: string
        description: UUID de la ubicación.
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - in: query
        name: product_id
        schema:
          type: string
        description: UUID del producto.
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      - in: query
        name: severity
        schema:
          type: string
      tags:
      - alerts
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAlertList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/alerts/{id}/:
    get:
      operationId: alerts_retrieve
      description: Obtiene el detalle de una alerta específica.
      summary: Detalle de alerta
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - alerts
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Alert'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/alerts/{id}/resolve/:
    post:
      operationId: alerts_resolve_create
      description: Marca una alerta como resuelta.
      summary: Resolver alerta
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - alerts
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Alert'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/alerts/history/:
    get:
      operationId: alerts_history_list
      description: Lista las alertas resueltas dentro de los filtros aplicados.
      summary: Historial de alertas resueltas
      parameters:
      - in: query
        name: alert_type
        schema:
          type: string
      - in: query
        name: category
        schema:
          type: string
      - in: query
        name: date_from
        schema:
          type: string
        description: Fecha desde (YYYY-MM-DD).
      - in: query
        name: date_to
        schema:
          type: string
        description: Fecha hasta (YYYY-MM-DD).
      - in: query
        name: location_id
        schema:
          type: string
        description: UUID de la ubicación.
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - in: query
        name: product_id
        schema:
          type: string
        description: UUID del producto.
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      - in: query
        name: severity
        schema:
          type: string
      tags:
      - alerts
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAlertList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/alerts/poll/:
    get:
      operationId: alerts_poll_list
      description: Consulta alertas nuevas desde un timestamp dado.
      summary: Polling de alertas
      parameters:
      - in: query
        name: severity
        schema:
          type: string
        description: 'Filtrar por severidades separadas por coma. Ej: CRITICA,ALTA'
      - in: query
        name: since
        schema:
          type: string
        description: 'ISO-8601 timestamp. Retorna alertas creadas después de este
          momento. Default: últimas 24 horas.'
      tags:
      - alerts
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Alert'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/alerts/stats/:
    get:
      operationId: alerts_stats_retrieve
      description: Obtiene métricas resumidas de alertas activas.
      summary: Métricas de alertas
      tags:
      - alerts
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                additionalProperties: {}
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/audit/:
    get:
      operationId: audit_list
      description: Lista los registros de auditoría con filtros opcionales.
      summary: Listar auditoría
      parameters:
      - in: query
        name: end
        schema:
          type: string
      - in: query
        name: event_type
        schema:
          type: string
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      - in: query
        name: start
        schema:
          type: string
      - in: query
        name: user_id
        schema:
          type: integer
      tags:
      - audit
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAuditLogList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/audit/{id}/:
    get:
      operationId: audit_retrieve
      description: Obtiene el detalle de un evento de auditoría.
      summary: Detalle de evento de auditoría
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - audit
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLog'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/auth/health/:
    get:
      operationId: auth_health_retrieve
      description: Comprobación de disponibilidad del servicio (sin autenticación).
      summary: Health check
      tags:
      - system
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
          description: ''
  /api/v1/auth/login/:
    post:
      operationId: auth_login_create
      description: 'RF-001, BR-03 — Envíe `password` y `username` **o** `email`. Respuesta:
        `access`, `refresh` y objeto `user` (perfil sin contraseña). Los auxiliares
        de despacho solo pueden autenticarse en franja operativa.'
      summary: Iniciar sesión (JWT)
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LoginRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LoginRequestRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  access:
                    type: string
                  refresh:
                    type: string
                  user:
                    type: object
          description: Tokens JWT y perfil de usuario.
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Error interno del servidor.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/auth/logout/:
    post:
      operationId: auth_logout_create
      description: RF-002 — Invalida el refresh token enviado en el cuerpo.
      summary: Cerrar sesión (blacklist refresh)
      tags:
      - auth
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: Sesión cerrada correctamente.
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/auth/me/:
    get:
      operationId: auth_me_retrieve
      description: Devuelve el perfil del usuario autenticado sin exponer la contraseña.
      summary: Perfil del usuario autenticado
      tags:
      - auth
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/auth/token/refresh/:
    post:
      operationId: auth_token_refresh_create
      description: 'Envía el `refresh` emitido en el login para obtener un nuevo `access`.
        RF-001, BR-03 — Auxiliar de despacho: renovación rechazada fuera de franja
        operativa (07:00–12:00 y 14:00–17:00, America/Bogota).'
      summary: Renovar access token
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ICMTokenRefreshRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ICMTokenRefreshRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ICMTokenRefreshRequest'
        required: true
      responses:
        '200':
          description: Nuevo access token generado.
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/auth/users/:
    get:
      operationId: auth_users_list
      description: Almacenista o administrador. Vista de lectura protegida (RF-002,
        BR-02).
      summary: Listar usuarios
      tags:
      - auth
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: auth_users_create
      description: Solo almacenista. Requiere username, email, password y rol.
      summary: Crear usuario
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UserCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/auth/users/{id}/:
    get:
      operationId: auth_users_retrieve
      description: Obtiene el detalle de un usuario por su identificador.
      summary: Detalle de usuario
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - auth
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      operationId: auth_users_update
      description: Solo almacenista.
      summary: Actualizar usuario
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/UserRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: auth_users_partial_update
      description: Solo almacenista.
      summary: Actualizar usuario (parcial)
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedUserRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedUserRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedUserRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/auth/users/{id}/disable/:
    post:
      operationId: auth_users_disable_create
      description: RF-002 — Revoca sesiones (blacklist) y marca la cuenta inactiva.
      summary: Deshabilitar usuario
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - auth
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: Usuario deshabilitado exitosamente.
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/auth/users/{id}/enable/:
    post:
      operationId: auth_users_enable_create
      description: RF-002 — Reactiva una cuenta de usuario previamente deshabilitada.
      summary: Rehabilitar usuario
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - auth
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/categories/:
    get:
      operationId: catalog_categories_list
      description: Lista las categorías registradas en el catálogo.
      summary: Listar categorías
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCategoryList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: catalog_categories_create
      description: Crea una nueva categoría de catálogo.
      summary: Crear categoría
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoryCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CategoryCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CategoryCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/categories/{id}/:
    get:
      operationId: catalog_categories_retrieve
      description: Obtiene el detalle de una categoría.
      summary: Detalle de categoría
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      operationId: catalog_categories_update
      description: Reemplaza los datos editables de una categoría.
      summary: Actualizar categoría
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoryUpdateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CategoryUpdateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CategoryUpdateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: catalog_categories_partial_update
      description: Actualiza solo los campos enviados de una categoría.
      summary: Actualizar categoría parcialmente
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedCategoryUpdateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedCategoryUpdateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedCategoryUpdateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: catalog_categories_destroy
      description: Marca la categoría como inactiva. El registro NO se elimina de
        la base de datos. Devuelve HTTP 409 si la categoría tiene productos activos
        asociados. Para reactivarla use POST /categories/{id}/restore/.
      summary: Desactivar categoría
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/categories/{id}/restore/:
    post:
      operationId: catalog_categories_restore_create
      description: Reactiva una categoría previamente desactivada.
      summary: Restaurar categoría
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/combos/:
    get:
      operationId: catalog_combos_list
      description: Lista los combos de productos registrados.
      summary: Listar combos
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedComboList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: catalog_combos_create
      description: Crea un nuevo combo de productos.
      summary: Crear combo
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ComboCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ComboCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ComboCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Combo'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/combos/{id}/:
    get:
      operationId: catalog_combos_retrieve
      description: Obtiene el detalle de un combo de productos.
      summary: Detalle de combo
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Combo'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      operationId: catalog_combos_update
      description: Reemplaza los datos de un combo. Si se envían `items`, reemplaza
        toda la lista de componentes.
      summary: Actualizar combo
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ComboUpdateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ComboUpdateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ComboUpdateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Combo'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: catalog_combos_partial_update
      description: Actualiza solo los campos enviados. Si se envían `items`, reemplaza
        toda la lista.
      summary: Actualizar combo parcialmente
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedComboUpdateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedComboUpdateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedComboUpdateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Combo'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: catalog_combos_destroy
      description: Marca el combo como inactivo. El registro NO se elimina de la base
        de datos ni afecta sus productos componentes. Para reactivarlo use POST /combos/{id}/restore/.
      summary: Desactivar combo
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/combos/{id}/restore/:
    post:
      operationId: catalog_combos_restore_create
      description: Reactiva un combo de productos previamente desactivado.
      summary: Restaurar combo
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Combo'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/products/:
    get:
      operationId: catalog_products_list
      description: Lista los productos disponibles en el catálogo.
      summary: Listar productos
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedProductList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: catalog_products_create
      description: Registra un nuevo producto en el catálogo.
      summary: Crear producto
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ProductCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ProductCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/products/{id}/:
    get:
      operationId: catalog_products_retrieve
      description: Obtiene el detalle completo de un producto.
      summary: Detalle de producto
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductDetail'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      operationId: catalog_products_update
      description: Reemplaza la información editable del producto.
      summary: Actualizar producto
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ProductRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ProductRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductDetail'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: catalog_products_partial_update
      description: Actualiza solo los campos enviados del producto.
      summary: Actualizar producto parcialmente
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedProductRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedProductRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedProductRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductDetail'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: catalog_products_destroy
      description: Marca el producto como inactivo. El registro NO se elimina de la
        base de datos ni afecta el historial de movimientos. Devuelve HTTP 409 si
        el producto pertenece a uno o más combos activos. Para reactivarlo use POST
        /products/{id}/restore/.
      summary: Desactivar producto
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflicto de estado (ej. stock insuficiente, o regla de negocio
            restrictiva).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/products/{id}/barcode/:
    get:
      operationId: catalog_products_barcode_retrieve
      description: Devuelve el payload completo del barcode para que el frontend lo
        consuma sin reconstruir SVG ni metadata.
      summary: Obtener barcode de producto
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductBarcode'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/products/{id}/prices/:
    get:
      operationId: catalog_products_prices_list
      description: Retorna el historial inmutable de cambios de precio del producto.
      summary: Historial de precios de un producto
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProductPriceHistory'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: catalog_products_prices_partial_update
      description: Actualiza unit_cost, sale_price_retail, sale_price_wholesale, tax_rate_pct
        y/o currency. Solo los campos enviados se modifican. Registra historial inmutable
        de cada cambio (BR-17).
      summary: Configurar precios de un producto
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedProductPriceUpdateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedProductPriceUpdateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedProductPriceUpdateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/products/{id}/restore/:
    post:
      operationId: catalog_products_restore_create
      description: Reactiva un producto previamente desactivado.
      summary: Restaurar producto
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductDetail'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/products/resolve/:
    get:
      operationId: catalog_products_resolve_retrieve
      description: Resuelve un identificador flexible a un producto del catálogo.
      summary: Resolver identificador de producto
      parameters:
      - in: query
        name: identifier
        schema:
          type: string
        description: Alias de `q` (misma semántica).
      - in: query
        name: q
        schema:
          type: string
        description: SKU, código de barras o fragmento de nombre.
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductDetail'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/resolve/:
    get:
      operationId: catalog_resolve_retrieve
      description: Resuelve un identificador flexible a un producto del catálogo.
      summary: Resolver identificador de producto
      parameters:
      - in: query
        name: identifier
        schema:
          type: string
        description: Alias de `q` (misma semántica).
      - in: query
        name: q
        schema:
          type: string
        description: SKU, código de barras o fragmento de nombre.
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductDetail'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/subcategories/:
    get:
      operationId: catalog_subcategories_list
      description: Lista las subcategorías registradas en el catálogo.
      summary: Listar subcategorías
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedSubcategoryList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: catalog_subcategories_create
      description: Crea una nueva subcategoría asociada a una categoría.
      summary: Crear subcategoría
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubcategoryCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SubcategoryCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SubcategoryCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subcategory'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/subcategories/{id}/:
    get:
      operationId: catalog_subcategories_retrieve
      description: Obtiene el detalle de una subcategoría.
      summary: Detalle de subcategoría
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subcategory'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      operationId: catalog_subcategories_update
      description: Reemplaza los datos editables de una subcategoría.
      summary: Actualizar subcategoría
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubcategoryUpdateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SubcategoryUpdateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SubcategoryUpdateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subcategory'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: catalog_subcategories_partial_update
      description: Actualiza solo los campos enviados de una subcategoría.
      summary: Actualizar subcategoría parcialmente
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedSubcategoryUpdateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedSubcategoryUpdateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedSubcategoryUpdateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subcategory'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: catalog_subcategories_destroy
      description: Marca la subcategoría como inactiva. El registro NO se elimina
        de la base de datos. Devuelve HTTP 409 si la subcategoría tiene productos
        activos asociados. Para reactivarla use POST /subcategories/{id}/restore/.
      summary: Desactivar subcategoría
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/catalog/subcategories/{id}/restore/:
    post:
      operationId: catalog_subcategories_restore_create
      description: Reactiva una subcategoría previamente desactivada.
      summary: Restaurar subcategoría
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - catalog
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subcategory'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/dashboard/alerts/:
    get:
      operationId: dashboard_alerts_retrieve
      description: Devuelve el resumen de alertas del dashboard.
      summary: Resumen de alertas
      parameters:
      - in: query
        name: expiring_days
        schema:
          type: integer
      - in: query
        name: period_days
        schema:
          type: integer
      tags:
      - dashboard
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardAlertSummary'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/dashboard/kpis/:
    get:
      operationId: dashboard_kpis_retrieve
      description: Devuelve el resumen de KPI del dashboard.
      summary: Resumen de KPI
      parameters:
      - in: query
        name: period_days
        schema:
          type: integer
      tags:
      - dashboard
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardKPISummary'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/dashboard/metrics/:
    get:
      operationId: dashboard_metrics_retrieve
      description: Devuelve las métricas operativas del dashboard.
      summary: Métricas operativas
      parameters:
      - in: query
        name: period_days
        schema:
          type: integer
      tags:
      - dashboard
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardMetricSummary'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/dashboard/movements/:
    get:
      operationId: dashboard_movements_list
      description: Devuelve los movimientos recientes visibles en el dashboard.
      summary: Movimientos recientes
      parameters:
      - in: query
        name: limit
        schema:
          type: integer
      - in: query
        name: period_days
        schema:
          type: integer
      tags:
      - dashboard
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DashboardMovement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/dashboard/overview/:
    get:
      operationId: dashboard_overview_retrieve
      description: Devuelve el resumen general del dashboard ejecutivo.
      summary: Resumen general del dashboard
      parameters:
      - in: query
        name: movements_limit
        schema:
          type: integer
        description: Cantidad máxima de movimientos recientes.
      - in: query
        name: period_days
        schema:
          type: integer
        description: Ventana histórica para el overview del dashboard.
      tags:
      - dashboard
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardOverview'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/:
    get:
      operationId: inventory_retrieve
      description: Devuelve el inventario consolidado por producto con filtros y exportación.
      summary: Inventario consolidado
      parameters:
      - in: query
        name: category_id
        schema:
          type: string
      - in: query
        name: location_id
        schema:
          type: string
      - in: query
        name: only_in_stock
        schema:
          type: boolean
      - in: query
        name: operational_status
        schema:
          type: string
        description: Filtra productos con stock en ubicaciones con este estado operativo.
      - in: query
        name: stock_below_reorder
        schema:
          type: boolean
      - in: query
        name: storage_type_id
        schema:
          type: string
        description: Filtra productos con stock en ubicaciones del tipo dado.
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          description: Inventario listado exitosamente.
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/locations/:
    get:
      operationId: inventory_locations_list
      description: Lista las ubicaciones registradas.
      summary: Listar ubicaciones
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Location'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: inventory_locations_create
      description: Crea una nueva ubicación de inventario.
      summary: Crear ubicación
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LocationCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LocationCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/locations/{id}/:
    get:
      operationId: inventory_locations_retrieve
      description: Obtiene el detalle de una ubicación.
      summary: Detalle de ubicación
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      operationId: inventory_locations_update
      description: Actualiza una ubicación.
      summary: Actualizar ubicación
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LocationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LocationRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: inventory_locations_partial_update
      description: Actualiza parcialmente una ubicación.
      summary: Actualizar ubicación parcialmente
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedLocationRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedLocationRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedLocationRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: inventory_locations_destroy
      description: Marca la ubicación como inactiva. El registro NO se elimina de
        la base de datos ni afecta el historial de movimientos.
      summary: Desactivar ubicación
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: Ubicación desactivada exitosamente.
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/locations/{id}/state-transitions/:
    post:
      operationId: inventory_locations_state_transitions_create
      description: Cambia el estado operativo de una ubicación.
      summary: Cambiar estado de ubicación
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationStateTransitionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LocationStateTransitionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LocationStateTransitionRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/products/{product_id}/stock/:
    get:
      operationId: inventory_products_stock_retrieve
      description: Consulta el stock consolidado de un producto por ubicación.
      summary: Stock por producto
      parameters:
      - in: path
        name: product_id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockByProductResponse'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/reconstruct/:
    post:
      operationId: inventory_reconstruct_create
      description: Reconstruye el stock derivado desde el ledger para un producto
        y ubicación.
      summary: Reconstruir stock
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StockReconstructRequestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StockReconstructRequestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StockReconstructRequestRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockReconstructResponse'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/search/:
    get:
      operationId: inventory_search_retrieve
      description: Busca productos por SKU, código de barras o nombre.
      summary: Buscar productos
      parameters:
      - in: query
        name: category
        schema:
          type: string
      - in: query
        name: q
        schema:
          type: string
        description: Texto para SKU, código de barras o nombre.
      - in: query
        name: subcategory
        schema:
          type: string
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InventoryPaginatedProductList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/stock/{id}/threshold/:
    patch:
      operationId: inventory_stock_threshold_partial_update
      description: Actualiza el umbral de reorden de un stock por ubicación.
      summary: Actualizar umbral de reorden
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStockThresholdRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStockThresholdRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStockThresholdRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockByLocation'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/stock/location/{location_id}/:
    get:
      operationId: inventory_stock_location_retrieve
      description: Consulta el stock de una ubicación específica.
      summary: Stock por ubicación
      parameters:
      - in: path
        name: location_id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InventoryPaginatedStockByLocationList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/stock/product/{product_id}/:
    get:
      operationId: inventory_stock_product_retrieve
      description: Consulta el stock consolidado de un producto por ubicación.
      summary: Stock por producto
      parameters:
      - in: path
        name: product_id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockByProductResponse'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/storage-templates/:
    get:
      operationId: inventory_storage_templates_list
      description: Lista las plantillas de almacenamiento registradas.
      summary: Listar plantillas de almacenamiento
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/StorageTemplate'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: inventory_storage_templates_create
      description: Crea una nueva plantilla de almacenamiento.
      summary: Crear plantilla de almacenamiento
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StorageTemplateCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StorageTemplateCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StorageTemplateCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageTemplate'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/storage-templates/{id}/:
    get:
      operationId: inventory_storage_templates_retrieve
      description: Obtiene el detalle de una plantilla de almacenamiento.
      summary: Detalle de plantilla de almacenamiento
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageTemplate'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      operationId: inventory_storage_templates_update
      description: Reemplaza completamente los datos de una plantilla de almacenamiento.
      summary: Reemplazar plantilla de almacenamiento
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StorageTemplateCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StorageTemplateCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StorageTemplateCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageTemplate'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: inventory_storage_templates_partial_update
      description: Actualiza parcialmente una plantilla de almacenamiento.
      summary: Actualizar plantilla de almacenamiento
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStorageTemplateCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStorageTemplateCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStorageTemplateCreateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageTemplate'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: inventory_storage_templates_destroy
      description: Marca la plantilla como inactiva. El registro NO se elimina de
        la base de datos.
      summary: Desactivar plantilla de almacenamiento
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: Plantilla de almacenamiento desactivada.
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/storage-types/:
    get:
      operationId: inventory_storage_types_list
      description: Lista los tipos de almacenamiento registrados.
      summary: Listar tipos de almacenamiento
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/StorageType'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: inventory_storage_types_create
      description: Crea un nuevo tipo de almacenamiento.
      summary: Crear tipo de almacenamiento
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StorageTypeCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StorageTypeCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StorageTypeCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageType'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/inventory/storage-types/{id}/:
    get:
      operationId: inventory_storage_types_retrieve
      description: Obtiene el detalle de un tipo de almacenamiento.
      summary: Detalle de tipo de almacenamiento
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageType'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      operationId: inventory_storage_types_update
      description: Actualiza un tipo de almacenamiento.
      summary: Actualizar tipo de almacenamiento
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StorageTypeCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StorageTypeCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StorageTypeCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageType'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: inventory_storage_types_partial_update
      description: Actualiza parcialmente un tipo de almacenamiento.
      summary: Actualizar tipo de almacenamiento parcialmente
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStorageTypeCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStorageTypeCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStorageTypeCreateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StorageType'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: inventory_storage_types_destroy
      description: Marca el tipo de almacenamiento como inactivo. El registro NO se
        elimina de la base de datos.
      summary: Desactivar tipo de almacenamiento
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - inventory
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: Tipo de almacenamiento desactivado exitosamente.
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/:
    get:
      operationId: movements_list
      description: Lista los movimientos del ledger con filtros opcionales.
      summary: Listar movimientos
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedMovementList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/{id}/:
    get:
      operationId: movements_retrieve
      description: Obtiene el detalle de un movimiento del ledger.
      summary: Detalle de movimiento
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/{id}/corrections/:
    post:
      operationId: movements_corrections_create
      description: Corrige un movimiento usando su identificador en la URL.
      summary: Corrección de movimiento (URL)
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - movements
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CorrectionCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CorrectionCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CorrectionCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '405':
          description: Método no permitido o registro inmutable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflicto de estado (ej. stock insuficiente, o regla de negocio
            restrictiva).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/adjustments/:
    get:
      operationId: movements_adjustments_list
      description: Lista los ajustes de inventario registrados.
      summary: Listar ajustes
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedMovementList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: movements_adjustments_create
      description: Registra un ajuste de inventario.
      summary: Registrar ajuste
      tags:
      - movements
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdjustmentCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AdjustmentCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AdjustmentCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflicto de estado (ej. stock insuficiente, o regla de negocio
            restrictiva).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/adjustments/correct/:
    post:
      operationId: movements_adjustments_correct_create
      description: Corrige un ajuste dentro de la ventana permitida por la regla de
        negocio.
      summary: Corregir ajuste dentro de ventana de tiempo
      tags:
      - movements
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdjustmentCorrectionRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AdjustmentCorrectionRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AdjustmentCorrectionRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '405':
          description: Método no permitido o registro inmutable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflicto de estado (ej. stock insuficiente, o regla de negocio
            restrictiva).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/combo-dispatch/:
    post:
      operationId: movements_combo_dispatch_create
      description: Despacha un combo completo y descuenta stock por cada ítem.
      summary: Despachar combo (descuenta stock por ítem)
      tags:
      - movements
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ComboDispatchRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ComboDispatchRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ComboDispatchRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflicto de estado (ej. stock insuficiente, o regla de negocio
            restrictiva).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/dispatches/:
    get:
      operationId: movements_dispatches_list
      description: Lista los despachos registrados en el ledger.
      summary: Listar despachos
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedMovementList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: movements_dispatches_create
      description: Registra un nuevo despacho de inventario (venta menor, venta mayor,
        daño o vencimiento).
      summary: Registrar despacho
      tags:
      - movements
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DispatchCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DispatchCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DispatchCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflicto de estado (ej. stock insuficiente, o regla de negocio
            restrictiva).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/dispatches/{id}/:
    get:
      operationId: movements_dispatches_retrieve
      description: Obtiene el detalle de un despacho registrado.
      summary: Detalle de despacho
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/dispatches/{id}/invoice/:
    get:
      operationId: movements_dispatches_invoice_retrieve
      description: Descarga el PDF de factura asociado a un despacho.
      summary: Descargar PDF de factura
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/entries/:
    get:
      operationId: movements_entries_list
      description: Lista las entradas de inventario registradas.
      summary: Listar entradas
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedMovementList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: movements_entries_create
      description: Registra una nueva entrada de inventario.
      summary: Registrar entrada
      tags:
      - movements
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EntryCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/EntryCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/EntryCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflicto de estado (ej. stock insuficiente, o regla de negocio
            restrictiva).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/entries/{id}/:
    get:
      operationId: movements_entries_retrieve
      description: Obtiene el detalle de una entrada de inventario.
      summary: Detalle de entrada
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/invoices/{number}/:
    get:
      operationId: movements_invoices_retrieve
      description: Retorna el detalle de una factura por número, incluyendo totales
        y lista de movimientos asociados.
      summary: Detalle de factura comercial
      parameters:
      - in: path
        name: number
        schema:
          type: string
        required: true
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invoice'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/invoices/{number}/pdf/:
    get:
      operationId: movements_invoices_pdf_retrieve
      description: Descarga el PDF de la factura con todos los datos del despacho,
        precios e información del cliente.
      summary: Descargar PDF de factura comercial
      parameters:
      - in: path
        name: number
        schema:
          type: string
        required: true
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/returns/:
    get:
      operationId: movements_returns_list
      description: Lista las devoluciones registradas en el ledger.
      summary: Listar devoluciones
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedMovementList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: movements_returns_create
      description: Registra una devolución de inventario.
      summary: Registrar devolución
      tags:
      - movements
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReturnCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ReturnCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReturnCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflicto de estado (ej. stock insuficiente, o regla de negocio
            restrictiva).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/movements/transfers/:
    get:
      operationId: movements_transfers_list
      description: Lista los traslados internos registrados.
      summary: Listar traslados
      parameters:
      - name: ordering
        required: false
        in: query
        description: Qué campo usar para ordenar los resultados.
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: Un número de página dentro del conjunto de resultados paginado.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Número de resultados a devolver por página.
        schema:
          type: integer
      - name: search
        required: false
        in: query
        description: Un término de búsqueda.
        schema:
          type: string
      tags:
      - movements
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedMovementList'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: movements_transfers_create
      description: Registra un traslado interno entre ubicaciones.
      summary: Registrar traslado
      tags:
      - movements
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransferCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TransferCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TransferCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflicto de estado (ej. stock insuficiente, o regla de negocio
            restrictiva).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/data/:
    get:
      operationId: reports_data_retrieve
      description: Entrega un dataset canónico para exportaciones del frontend.
      summary: Dataset de reportes
      parameters:
      - in: query
        name: days
        schema:
          type: integer
      - in: query
        name: end
        schema:
          type: string
        description: Fin del rango (ISO-8601).
      - in: query
        name: invoice_number
        schema:
          type: string
      - in: query
        name: kind
        schema:
          type: string
        description: 'Tipo de dataset: inventory-summary, movements-summary, movements-report,
          warehouse-utilization, warehouse-occupancy-distribution, quality-operational,
          discard-operational, dispatch-operational, movements-history, sales-summary,
          top-products, invoices, kpi, expiring.'
        required: true
      - in: query
        name: limit
        schema:
          type: integer
      - in: query
        name: period_days
        schema:
          type: integer
      - in: query
        name: product_id
        schema:
          type: string
        description: UUID del producto.
      - in: query
        name: start
        schema:
          type: string
        description: Inicio del rango (ISO-8601).
      - in: query
        name: type
        schema:
          type: string
      - in: query
        name: user_id
        schema:
          type: integer
        description: ID del usuario operario.
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportDataset'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/discard-operational/:
    get:
      operationId: reports_discard_operational_retrieve
      description: Resume indicadores operativos de descarte.
      summary: Resumen operativo de descarte
      parameters:
      - in: query
        name: period_days
        schema:
          type: integer
        description: Ventana histórica en días para el resumen de descarte.
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiscardOperationalSummary'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/dispatch-operational/:
    get:
      operationId: reports_dispatch_operational_retrieve
      description: 'Resumen operativo de despacho. Campos clave: `period`, `sales`,
        `invoice_linked_dispatches`, `shipments`, `invoice_linked_ratio`, `movement_counts`,
        `top_products`, `order_proxy`, `carriers`. `order_proxy` y `carriers` son
        campos extensibles para integrar información por pedido o transportadora cuando
        esos dominios existan.'
      summary: Resumen operativo de despacho
      parameters:
      - in: query
        name: period_days
        schema:
          type: integer
        description: Ventana histórica en días para el resumen operativo.
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DispatchOperationalSummary'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/dispatch-operational/orders/:
    get:
      operationId: reports_dispatch_operational_orders_list
      description: Muestra muestras de despachos por pedido y factura.
      summary: Muestras de despachos por pedido
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Fin del rango (ISO-8601).
      - in: query
        name: invoice_number
        schema:
          type: string
      - in: query
        name: start
        schema:
          type: string
        description: Inicio del rango (ISO-8601).
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PerOrderSample'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/expiring/:
    get:
      operationId: reports_expiring_list
      description: Lista productos o lotes próximos a vencer.
      summary: Productos próximos a vencer
      parameters:
      - in: query
        name: days
        schema:
          type: integer
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ExpiringLotReportItem'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/inventory/summary/:
    get:
      operationId: reports_inventory_summary_retrieve
      description: Resume el inventario actual por producto y ubicación.
      summary: Resumen de inventario
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InventorySummaryResponse'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/invoices/:
    get:
      operationId: reports_invoices_list
      description: Lista las facturas asociadas a movimientos en el rango consultado.
      summary: Historial de facturas
      parameters:
      - in: query
        name: end
        schema:
          type: string
      - in: query
        name: invoice_number
        schema:
          type: string
      - in: query
        name: product_id
        schema:
          type: string
        description: UUID del producto.
      - in: query
        name: start
        schema:
          type: string
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Movement'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/kpi/:
    get:
      operationId: reports_kpi_retrieve
      description: Devuelve el panel legado de KPI para compatibilidad.
      summary: Panel KPI legado
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KpiDashboard'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/margin-by-product/:
    get:
      operationId: reports_margin_by_product_retrieve
      description: Retorna revenue, COGS, margen bruto y margen % por SKU para despachos
        de venta. Ordenado de mayor a menor revenue.
      summary: Margen bruto por producto
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Fin del período (ISO-8601).
      - in: query
        name: limit
        schema:
          type: integer
        description: Máximo de SKUs (default 50).
      - in: query
        name: start
        schema:
          type: string
        description: Inicio del período (ISO-8601).
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/movements/history/:
    get:
      operationId: reports_movements_history_retrieve
      description: Devuelve el historial de movimientos con filtros y exportación.
      summary: Historial de movimientos
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Filtro superior de fecha (ISO-8601).
      - in: query
        name: location_id
        schema:
          type: string
        description: UUID de la ubicación (origen o destino).
      - in: query
        name: product_id
        schema:
          type: string
        description: UUID del producto.
      - in: query
        name: start
        schema:
          type: string
        description: Filtro inferior de fecha (ISO-8601).
      - in: query
        name: user_id
        schema:
          type: integer
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          description: Historial de movimientos.
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/movements/report/:
    get:
      operationId: reports_movements_report_retrieve
      description: Reporte de movimientos filtrado por rango y tipo.
      summary: Reporte de movimientos
      parameters:
      - in: query
        name: end
        schema:
          type: string
        required: true
      - in: query
        name: start
        schema:
          type: string
        required: true
      - in: query
        name: type
        schema:
          type: string
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovementReportResponse'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/movements/summary/:
    get:
      operationId: reports_movements_summary_retrieve
      description: Resume la cantidad de movimientos por tipo en un rango dado.
      summary: Resumen de movimientos
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Fin del rango (ISO-8601).
        required: true
      - in: query
        name: start
        schema:
          type: string
        description: Inicio del rango (ISO-8601).
        required: true
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovementSummaryResponse'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/quality-operational/:
    get:
      operationId: reports_quality_operational_retrieve
      description: Resume indicadores operativos de calidad.
      summary: Resumen operativo de calidad
      parameters:
      - in: query
        name: period_days
        schema:
          type: integer
        description: Ventana histórica en días para el resumen operativo.
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QualityOperationalResponse'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/revenue-summary/:
    get:
      operationId: reports_revenue_summary_retrieve
      description: 'Retorna subtotales, IVA y totales de ventas al por menor y mayor,
        más un combinado. Período default: últimos 30 días.'
      summary: Revenue por tipo de venta
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Fin del período (ISO-8601).
      - in: query
        name: start
        schema:
          type: string
        description: Inicio del período (ISO-8601).
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/sales-by-customer/:
    get:
      operationId: reports_sales_by_customer_retrieve
      description: Retorna revenue, unidades y número de órdenes de venta al por mayor
        agrupados por cliente.
      summary: Ventas por cliente
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Fin del período (ISO-8601).
      - in: query
        name: limit
        schema:
          type: integer
        description: Máximo de clientes (default 50).
      - in: query
        name: start
        schema:
          type: string
        description: Inicio del período (ISO-8601).
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/sales/summary/:
    get:
      operationId: reports_sales_summary_retrieve
      description: Resume las ventas vinculadas a despachos en un rango dado.
      summary: Resumen de ventas
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Fin del rango (ISO-8601).
        required: true
      - in: query
        name: start
        schema:
          type: string
        description: Inicio del rango (ISO-8601).
        required: true
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalesSummaryResponse'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/top-products/:
    get:
      operationId: reports_top_products_list
      description: Lista los productos más despachados en el periodo indicado.
      summary: Top productos despachados
      parameters:
      - in: query
        name: limit
        schema:
          type: integer
      - in: query
        name: period_days
        schema:
          type: integer
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TopDispatchedProduct'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/reports/warehouse-utilization/:
    get:
      operationId: reports_warehouse_utilization_retrieve
      description: Devuelve la utilización agregada de bodegas.
      summary: Utilización de bodegas
      tags:
      - reports
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WarehouseUtilizationResponse'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/webhooks/deliveries/:
    get:
      operationId: webhooks_deliveries_list
      description: Lista el historial de entregas de webhooks.
      summary: Historial de entregas de webhook
      tags:
      - webhooks
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WebhookDelivery'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/webhooks/endpoints/:
    get:
      operationId: webhooks_endpoints_list
      description: Lista los endpoints de webhook configurados.
      summary: Listar endpoints de webhook
      tags:
      - webhooks
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WebhookEndpoint'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      operationId: webhooks_endpoints_create
      description: Crea un nuevo endpoint de webhook.
      summary: Crear endpoint de webhook
      tags:
      - webhooks
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookEndpointCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WebhookEndpointCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WebhookEndpointCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpoint'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/webhooks/endpoints/{id}/:
    get:
      operationId: webhooks_endpoints_retrieve
      description: Obtiene el detalle de un endpoint de webhook.
      summary: Detalle de endpoint de webhook
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - webhooks
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpoint'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      operationId: webhooks_endpoints_update
      description: Reemplaza completamente los datos de un endpoint de webhook.
      summary: Reemplazar endpoint de webhook
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - webhooks
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookEndpointCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WebhookEndpointCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WebhookEndpointCreateRequest'
        required: true
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpoint'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: webhooks_endpoints_partial_update
      description: Actualiza parcialmente un endpoint de webhook.
      summary: Actualizar endpoint de webhook
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - webhooks
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedWebhookEndpointCreateRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedWebhookEndpointCreateRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedWebhookEndpointCreateRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpoint'
          description: ''
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: webhooks_endpoints_destroy
      description: 'Marca el endpoint como inactivo: deja de recibir eventos. El registro
        NO se elimina de la base de datos ni se pierden las entregas registradas.'
      summary: Desactivar endpoint de webhook
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - webhooks
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '204':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/webhooks/endpoints/{id}/test/:
    post:
      operationId: webhooks_endpoints_test_create
      description: Envía un payload de prueba al endpoint de webhook.
      summary: Probar endpoint de webhook
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - webhooks
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookTestRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WebhookTestRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WebhookTestRequest'
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          description: No response body
        '400':
          description: Solicitud inválida o con errores de validación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: No autenticado o token inválido/expirado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Permiso denegado para ejecutar la acción.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Recurso no encontrado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Entidad no procesable (errores lógicos o de dominio ICM).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/v1/webhooks/stats/:
    get:
      operationId: webhooks_stats_retrieve
      description: Obtiene métricas agregadas de entregas de webhooks.
      summary: Métricas de webhooks
      tags:
      - webhooks
      security:
      - jwtAuth: []
      - BearerAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookStatsResponse'
          description: ''
components:
  schemas:
    AdjustmentCorrectionRequest:
      type: object
      description: Cuerpo para `POST .../adjustments/correct/` con id de movimiento.
      properties:
        movement_id:
          type: string
          format: uuid
        origin_id:
          type: string
          format: uuid
        destination_id:
          type: string
          format: uuid
        quantity:
          type: integer
          minimum: 1
      required:
      - destination_id
      - movement_id
      - origin_id
      - quantity
    AdjustmentCreateRequest:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        location_id:
          type: string
          format: uuid
        new_quantity:
          type: integer
          minimum: 0
        justification:
          type: string
      required:
      - justification
      - location_id
      - new_quantity
      - product_id
    Alert:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        product:
          type: string
          format: uuid
          readOnly: true
        product_sku:
          type: string
          readOnly: true
        lot:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        lot_code:
          type: string
          readOnly: true
        lot_expiration_date:
          type: string
          format: date
          readOnly: true
        location:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        alert_type:
          allOf:
          - $ref: '#/components/schemas/AlertTypeEnum'
          readOnly: true
        severity:
          allOf:
          - $ref: '#/components/schemas/SeverityEnum'
          readOnly: true
        category:
          allOf:
          - $ref: '#/components/schemas/CategoryEnum'
          readOnly: true
        message:
          type: string
          readOnly: true
        is_resolved:
          type: boolean
          readOnly: true
        resolved_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        resolved_by:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - alert_type
      - category
      - created_at
      - id
      - is_resolved
      - location
      - lot
      - lot_code
      - lot_expiration_date
      - message
      - product
      - product_sku
      - resolved_at
      - resolved_by
      - severity
    AlertTypeEnum:
      enum:
      - LOW_STOCK
      - EXPIRATION_30
      - EXPIRATION_60
      - LOT_EXPIRED
      - COLD_CHAIN_MISSING
      - LOCATION_BLOCKED_WITH_STOCK
      - STOCK_MISMATCH
      - STOCK_ZERO
      type: string
      description: |-
        * `LOW_STOCK` - Stock bajo mínimo
        * `EXPIRATION_30` - Vencimiento en 30 días
        * `EXPIRATION_60` - Vencimiento en 60 días
        * `LOT_EXPIRED` - Lote vencido
        * `COLD_CHAIN_MISSING` - Cadena de frío irregular
        * `LOCATION_BLOCKED_WITH_STOCK` - Ubicación bloqueada con stock
        * `STOCK_MISMATCH` - Desincronización stock vs ledger
        * `STOCK_ZERO` - Producto sin stock
    AuditLog:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        event_type:
          allOf:
          - $ref: '#/components/schemas/EventTypeEnum'
          readOnly: true
        user:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        ip_address:
          type: string
          readOnly: true
          nullable: true
        movement:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        description:
          type: string
          readOnly: true
        metadata:
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - description
      - event_type
      - id
      - ip_address
      - metadata
      - movement
      - user
    CapacityModeEnum:
      enum:
      - none
      - relative_scale
      - absolute_legacy
      type: string
      description: |-
        * `none` - Sin capacidad
        * `relative_scale` - Escala relativa
        * `absolute_legacy` - Capacidad absoluta (legacy)
    Category:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        name:
          type: string
          maxLength: 128
        slug:
          type: string
          maxLength: 128
          pattern: ^[-a-zA-Z0-9_]+$
        requires_serial_number:
          type: boolean
          description: 'BR-04: categoría exige número de serie en entradas/salidas.'
        is_returnable:
          type: boolean
          description: 'BR-05: categoría admite devoluciones (Electroterapia / electrónicos).'
        description:
          type: string
        is_active:
          type: boolean
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
      required:
      - created_at
      - id
      - name
      - slug
      - updated_at
    CategoryCreateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
          default: ''
        requires_serial_number:
          type: boolean
          default: false
        is_returnable:
          type: boolean
          default: false
      required:
      - name
    CategoryEnum:
      enum:
      - STOCK
      - EXPIRATION
      - LOCATION
      - INTEGRITY
      - BUSINESS
      type: string
      description: |-
        * `STOCK` - Stock
        * `EXPIRATION` - Vencimiento
        * `LOCATION` - Ubicación
        * `INTEGRITY` - Integridad
        * `BUSINESS` - Negocio
    CategoryUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        requires_serial_number:
          type: boolean
        is_returnable:
          type: boolean
    Combo:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        name:
          type: string
          maxLength: 255
        sku:
          type: string
          maxLength: 100
        is_active:
          type: boolean
        components:
          type: array
          items:
            $ref: '#/components/schemas/ComboItem'
          readOnly: true
        price_strategy:
          allOf:
          - $ref: '#/components/schemas/ComboPriceStrategyEnum'
          readOnly: true
          description: |-
            derived: suma de precios de componentes. fixed: precio fijo del combo.

            * `derived` - Derivado de componentes
            * `fixed` - Precio fijo del combo
        fixed_price_retail:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Precio fijo al por menor del combo (si price_strategy=fixed).
        fixed_price_wholesale:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Precio fijo al por mayor del combo (si price_strategy=fixed).
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
      required:
      - components
      - created_at
      - fixed_price_retail
      - fixed_price_wholesale
      - id
      - name
      - price_strategy
      - sku
      - updated_at
    ComboCreateItemRequest:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
          description: UUID del producto
        quantity:
          type: integer
          minimum: 1
          default: 1
          description: Cantidad del producto en el combo
      required:
      - product_id
    ComboCreateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        sku:
          type: string
          minLength: 1
        is_active:
          type: boolean
          default: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/ComboCreateItemRequest'
        price_strategy:
          allOf:
          - $ref: '#/components/schemas/PriceStrategyEnum'
          default: derived
        fixed_price_retail:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
        fixed_price_wholesale:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
      required:
      - items
      - name
      - sku
    ComboDispatchRequest:
      type: object
      description: Despacho de un combo completo desde una ubicación (RF-003, Opción
        B).
      properties:
        combo_id:
          type: string
          format: uuid
          description: UUID del combo a despachar.
        location_id:
          type: string
          format: uuid
          description: UUID de la ubicación desde donde se descuentan los productos.
      required:
      - combo_id
      - location_id
    ComboItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        product:
          type: string
          format: uuid
        quantity:
          type: integer
          maximum: 2147483647
          minimum: 0
      required:
      - id
      - product
    ComboPriceStrategyEnum:
      enum:
      - derived
      - fixed
      type: string
      description: |-
        * `derived` - Derivado de componentes
        * `fixed` - Precio fijo del combo
    ComboUpdateItemRequest:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        quantity:
          type: integer
          minimum: 1
          default: 1
      required:
      - product_id
    ComboUpdateRequest:
      type: object
      description: Campos editables de un combo. is_active no se acepta; usar DELETE/restore.
      properties:
        name:
          type: string
          minLength: 1
        sku:
          type: string
          minLength: 1
        items:
          type: array
          items:
            $ref: '#/components/schemas/ComboUpdateItemRequest'
        price_strategy:
          $ref: '#/components/schemas/PriceStrategyEnum'
        fixed_price_retail:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
        fixed_price_wholesale:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
    CorrectionCreateRequest:
      type: object
      properties:
        origin_id:
          type: string
          format: uuid
        destination_id:
          type: string
          format: uuid
        quantity:
          type: integer
          minimum: 1
      required:
      - destination_id
      - origin_id
      - quantity
    DashboardAlertSummary:
      type: object
      properties:
        active:
          type: integer
        reorder:
          type: integer
        expiring:
          type: integer
        expiring_days:
          type: integer
        returns:
          type: integer
      required:
      - active
      - expiring
      - expiring_days
      - reorder
      - returns
    DashboardKPISummary:
      type: object
      properties:
        warehouse_utilization:
          $ref: '#/components/schemas/DashboardKPIValue'
        damaged_rate:
          $ref: '#/components/schemas/DashboardKPIValue'
        return_rate:
          $ref: '#/components/schemas/DashboardKPIValue'
        dispatch_invoice_ratio:
          $ref: '#/components/schemas/DashboardKPIValue'
        discard_rate:
          $ref: '#/components/schemas/DashboardKPIValue'
        cold_chain_alerts:
          $ref: '#/components/schemas/DashboardKPIValue'
      required:
      - cold_chain_alerts
      - damaged_rate
      - discard_rate
      - dispatch_invoice_ratio
      - return_rate
      - warehouse_utilization
    DashboardKPIValue:
      type: object
      properties:
        label:
          type: string
        value:
          type: number
          format: double
          nullable: true
        unit:
          type: string
        precision:
          $ref: '#/components/schemas/PrecisionEnum'
        threshold:
          type: number
          format: double
          nullable: true
        source:
          type: string
      required:
      - label
      - precision
      - source
      - threshold
      - unit
      - value
    DashboardMetricSummary:
      type: object
      properties:
        stock_total:
          type: integer
        dispatches_today:
          type: integer
        reorder_count:
          type: integer
        invoices_issued:
          type: integer
        invoice_range:
          type: string
          nullable: true
      required:
      - dispatches_today
      - invoice_range
      - invoices_issued
      - reorder_count
      - stock_total
    DashboardMovement:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
        title:
          type: string
        sku:
          type: string
        quantity:
          type: integer
        user:
          type: string
        time:
          type: string
        status:
          type: string
      required:
      - id
      - quantity
      - sku
      - status
      - time
      - title
      - type
      - user
    DashboardOverview:
      type: object
      properties:
        metrics:
          $ref: '#/components/schemas/DashboardMetricSummary'
        alerts:
          $ref: '#/components/schemas/DashboardAlertSummary'
        kpis:
          $ref: '#/components/schemas/DashboardKPISummary'
        movements:
          type: array
          items:
            $ref: '#/components/schemas/DashboardMovement'
        generated_at:
          type: string
          format: date-time
      required:
      - alerts
      - generated_at
      - kpis
      - metrics
      - movements
    DiscardOperationalSummary:
      type: object
      properties:
        period:
          $ref: '#/components/schemas/QualityOperationalPeriod'
        totals:
          $ref: '#/components/schemas/QualityOperationalTotals'
        by_type:
          type: array
          items:
            $ref: '#/components/schemas/QualityOperationalType'
        by_product:
          type: array
          items:
            $ref: '#/components/schemas/QualityOperationalItem'
        notes:
          type: array
          items:
            type: string
      required:
      - by_product
      - by_type
      - notes
      - period
      - totals
    DispatchCreateRequest:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        location_id:
          type: string
          format: uuid
        quantity:
          type: integer
          minimum: 1
        movement_type:
          type: string
          minLength: 1
        lot_id:
          type: string
          format: uuid
          nullable: true
        scanned_code:
          type: string
          nullable: true
        order_sku:
          type: string
          nullable: true
        serial_number:
          type: string
          nullable: true
        customer_data: {}
        note:
          type: string
        cold_chain_acknowledged:
          type: boolean
          default: false
        electrical_safety_acknowledged:
          type: boolean
          default: false
        privacy_notice_acknowledged:
          type: boolean
          default: false
        discount_pct:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          nullable: true
          description: Descuento en porcentaje (0-100). Opcional — omitir si no aplica
            descuento. Solo tiene efecto si el producto tiene precio configurado.
      required:
      - location_id
      - movement_type
      - product_id
      - quantity
    DispatchOperationalSummary:
      type: object
      properties:
        period:
          $ref: '#/components/schemas/QualityOperationalPeriod'
        sales:
          $ref: '#/components/schemas/SalesTotals'
        invoice_linked_dispatches:
          type: integer
        movement_counts:
          type: object
          additionalProperties:
            type: integer
        top_products:
          type: array
          items:
            $ref: '#/components/schemas/TopDispatchedProduct'
        notes:
          type: array
          items:
            type: string
        shipments:
          type: integer
        invoice_linked_ratio:
          type: number
          format: double
        order_proxy:
          nullable: true
        carriers:
          nullable: true
        per_order_samples:
          type: array
          items: {}
        promised_date_example:
          type: string
          format: date-time
          nullable: true
      required:
      - carriers
      - invoice_linked_dispatches
      - invoice_linked_ratio
      - movement_counts
      - notes
      - order_proxy
      - per_order_samples
      - period
      - promised_date_example
      - sales
      - shipments
      - top_products
    EntryCreateRequest:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        location_id:
          type: string
          format: uuid
        quantity:
          type: integer
          minimum: 1
        lot_code:
          type: string
          nullable: true
        lot_expiration_date:
          type: string
          format: date
          nullable: true
        serial_number:
          type: string
          nullable: true
        qty_invoiced:
          type: integer
          nullable: true
        discrepancy_note:
          type: string
          nullable: true
        cold_chain_acknowledged:
          type: boolean
          default: false
        electrical_safety_acknowledged:
          type: boolean
          default: false
      required:
      - location_id
      - product_id
      - quantity
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
          description: Código de error del sistema o cliente (ej. 'VALIDATION_ERROR',
            'NOT_FOUND', 'INSUFFICIENT_STOCK').
        message:
          type: string
          description: Mensaje legible para el usuario que describe el error.
        detail:
          type: object
          description: Detalles adicionales, comúnmente un diccionario con errores
            específicos por campo o reglas.
          additionalProperties: true
      required:
      - error
      - message
      - detail
    EventTypeEnum:
      enum:
      - LOGIN_SUCCESS
      - LOGIN_FAILED
      - LOGOUT
      - USER_CREATED
      - USER_UPDATED
      - USER_DISABLED
      - USER_ENABLED
      - MOVEMENT_CREATED
      - ADJUSTMENT_CREATED
      - RETURN_CREATED
      - REPORT_GENERATED
      - PERMISSION_CHANGED
      - STOCK_RECONSTRUCTED
      - PRODUCT_CREATED
      - PRODUCT_UPDATED
      - PRODUCT_DEACTIVATED
      - PRODUCT_ACTIVATED
      - PRODUCT_PRICE_UPDATED
      - COMBO_CREATED
      - COMBO_UPDATED
      - COMBO_DEACTIVATED
      - COMBO_ACTIVATED
      - CATEGORY_CREATED
      - CATEGORY_UPDATED
      - CATEGORY_DEACTIVATED
      - CATEGORY_ACTIVATED
      - SUBCATEGORY_CREATED
      - SUBCATEGORY_UPDATED
      - SUBCATEGORY_DEACTIVATED
      - SUBCATEGORY_ACTIVATED
      - MOVEMENT_CORRECTED
      - RETURN_APPROVED
      - RETURN_REJECTED
      - ALERT_ACKNOWLEDGED
      - UNAUTHORIZED_ACCESS_ATTEMPT
      - MODIFICATION_ATTEMPT_ON_IMMUTABLE_RECORD
      - INVOICE_GENERATED
      - DISPATCH_WITH_PRICE_COMPLETED
      type: string
      description: |-
        * `LOGIN_SUCCESS` - Login exitoso
        * `LOGIN_FAILED` - Login fallido
        * `LOGOUT` - Cierre de sesión
        * `USER_CREATED` - Usuario creado
        * `USER_UPDATED` - Usuario actualizado
        * `USER_DISABLED` - Usuario deshabilitado
        * `USER_ENABLED` - Usuario rehabilitado
        * `MOVEMENT_CREATED` - Movimiento creado
        * `ADJUSTMENT_CREATED` - Ajuste creado
        * `RETURN_CREATED` - Devolución registrada
        * `REPORT_GENERATED` - Reporte generado
        * `PERMISSION_CHANGED` - Permisos modificados
        * `STOCK_RECONSTRUCTED` - Stock reconstruido desde ledger
        * `PRODUCT_CREATED` - Producto creado
        * `PRODUCT_UPDATED` - Producto actualizado
        * `PRODUCT_DEACTIVATED` - Producto desactivado
        * `PRODUCT_ACTIVATED` - Producto reactivado
        * `PRODUCT_PRICE_UPDATED` - Precios de producto actualizados
        * `COMBO_CREATED` - Combo de productos creado
        * `COMBO_UPDATED` - Combo de productos actualizado
        * `COMBO_DEACTIVATED` - Combo de productos desactivado
        * `COMBO_ACTIVATED` - Combo de productos reactivado
        * `CATEGORY_CREATED` - Categoría de catálogo creada
        * `CATEGORY_UPDATED` - Categoría de catálogo actualizada
        * `CATEGORY_DEACTIVATED` - Categoría de catálogo desactivada
        * `CATEGORY_ACTIVATED` - Categoría de catálogo reactivada
        * `SUBCATEGORY_CREATED` - Subcategoría de catálogo creada
        * `SUBCATEGORY_UPDATED` - Subcategoría de catálogo actualizada
        * `SUBCATEGORY_DEACTIVATED` - Subcategoría de catálogo desactivada
        * `SUBCATEGORY_ACTIVATED` - Subcategoría de catálogo reactivada
        * `MOVEMENT_CORRECTED` - Movimiento corregido (BR-06)
        * `RETURN_APPROVED` - Devolución aprobada
        * `RETURN_REJECTED` - Devolución rechazada
        * `ALERT_ACKNOWLEDGED` - Alerta reconocida
        * `UNAUTHORIZED_ACCESS_ATTEMPT` - Intento de acceso no autorizado
        * `MODIFICATION_ATTEMPT_ON_IMMUTABLE_RECORD` - Intento de modificación sobre registro inmutable
        * `INVOICE_GENERATED` - Factura generada
        * `DISPATCH_WITH_PRICE_COMPLETED` - Despacho con precio completado
    ExpiringLotReportItem:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        sku:
          type: string
        name:
          type: string
        lot_id:
          type: string
          format: uuid
        lot_code:
          type: string
        expiration_date:
          type: string
          format: date
        days_left:
          type: integer
        location_id:
          type: string
          format: uuid
        location_code:
          type: string
        location_name:
          type: string
        available_quantity:
          type: integer
      required:
      - available_quantity
      - days_left
      - expiration_date
      - location_code
      - location_id
      - location_name
      - lot_code
      - lot_id
      - name
      - product_id
      - sku
    ICMTokenRefreshRequest:
      type: object
      description: |-
        RF-001, BR-03 — Renueva access; los auxiliares solo obtienen token en franja operativa.

        Alineado a criterios Gherkin en `docs/requisitos/ERS_ICM_Requisitos.md` (Feature inicio de sesión,
        Scenario 3: acceso bloqueado fuera de horario) y a `docs/requisitos/ICM_Informe_Elicitacion_v2_plus.docx.md`
        (BR-03: franjas 07:00–12:00 y 14:00–17:00 para auxiliares). La renovación JWT no debe eludir esa regla.
      properties:
        refresh:
          type: string
          minLength: 1
      required:
      - refresh
    InventoryPaginatedProductList:
      type: object
      description: Forma estándar DRF `PageNumberPagination` para listas de productos.
      properties:
        count:
          type: integer
        next:
          type: string
          nullable: true
        previous:
          type: string
          nullable: true
        results:
          type: array
          items:
            $ref: '#/components/schemas/Product'
      required:
      - count
      - results
    InventoryPaginatedStockByLocationList:
      type: object
      properties:
        count:
          type: integer
        next:
          type: string
          nullable: true
        previous:
          type: string
          nullable: true
        results:
          type: array
          items:
            $ref: '#/components/schemas/StockByLocation'
      required:
      - count
      - results
    InventorySummaryItem:
      type: object
      properties:
        category_id:
          type: string
          format: uuid
        category:
          type: string
        total_units:
          type: integer
      required:
      - category
      - category_id
      - total_units
    InventorySummaryResponse:
      type: object
      properties:
        by_category:
          type: array
          items:
            $ref: '#/components/schemas/InventorySummaryItem'
        products_with_zero_stock:
          type: integer
        approximate_value:
          type: number
          format: double
          nullable: true
        approximate_value_note:
          type: string
      required:
      - approximate_value
      - approximate_value_note
      - by_category
      - products_with_zero_stock
    Invoice:
      type: object
      properties:
        id:
          type: integer
        number:
          type: string
        customer_name:
          type: string
        customer_email:
          type: string
          format: email
        customer_phone:
          type: string
        customer_address:
          type: string
        subtotal:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,4})?$
        discount_total:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
        tax_total:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
        total_amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,4})?$
        currency:
          type: string
        pdf:
          type: string
          format: uri
          nullable: true
        issued_by:
          type: string
          description: Username del operador que generó la factura.
        issued_at:
          type: string
          format: date-time
        movement_ids:
          type: array
          items:
            type: string
          readOnly: true
          description: Lista de UUIDs de los movimientos agrupados en esta factura.
      required:
      - currency
      - customer_address
      - customer_email
      - customer_name
      - customer_phone
      - discount_total
      - id
      - issued_at
      - issued_by
      - movement_ids
      - number
      - pdf
      - subtotal
      - tax_total
      - total_amount
    KpiDashboard:
      type: object
      properties:
        movements_today:
          type: integer
        low_stock_products_count:
          type: integer
        active_alerts_unresolved:
          type: integer
        dispatches_this_month:
          type: integer
        generated_at:
          type: string
          format: date-time
      required:
      - active_alerts_unresolved
      - dispatches_this_month
      - generated_at
      - low_stock_products_count
      - movements_today
    Location:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        code:
          type: string
          maxLength: 100
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          maxLength: 100
        description:
          type: string
        is_retail:
          type: boolean
          description: True si la ubicación es un punto de venta minorista (vitrina,
            mostrador, etc.).
        max_capacity:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
          description: Capacidad máxima de productos. Aplica principalmente a vitrinas.
        storage_type_id:
          type: string
          format: uuid
          nullable: true
        storage_type_code:
          type: string
          readOnly: true
        storage_type_name:
          type: string
          readOnly: true
        storage_template_id:
          type: string
          format: uuid
          nullable: true
        storage_template_code:
          type: string
          readOnly: true
        storage_template_name:
          type: string
          readOnly: true
        operational_status:
          allOf:
          - $ref: '#/components/schemas/OperationalStatusEnum'
          description: |-
            Estado operativo de la ubicación para reglas de movimientos.

            * `active` - Activa
            * `maintenance` - Mantenimiento
            * `restricted` - Restringida
            * `blocked` - Bloqueada
            * `archived` - Archivada
        capacity_mode:
          allOf:
          - $ref: '#/components/schemas/CapacityModeEnum'
          description: |-
            Modo de capacidad para cálculo de ocupación informativa.

            * `none` - Sin capacidad
            * `relative_scale` - Escala relativa
            * `absolute_legacy` - Capacidad absoluta (legacy)
        capacity_level:
          type: integer
          maximum: 32767
          minimum: 0
          nullable: true
          description: Escala relativa (1-5) cuando capacity_mode=relative_scale.
        capacity_score:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
          description: Puntaje de capacidad relativo (unidad abstracta, no bloqueante).
        occupancy_estimate_pct:
          type: number
          format: double
          nullable: true
          description: Estimación informativa de ocupación calculada o ajustada.
        is_active:
          type: boolean
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
      required:
      - code
      - created_at
      - id
      - name
      - storage_template_code
      - storage_template_name
      - storage_type_code
      - storage_type_name
      - updated_at
    LocationCreateRequest:
      type: object
      description: Serializador de entrada para crear una ubicación (sin code, sin
        enum).
      properties:
        name:
          type: string
          minLength: 1
          description: Nombre de la ubicación (ej. 'Vitrina', 'Bodega Central').
        description:
          type: string
          default: ''
        is_retail:
          type: boolean
          nullable: true
          description: Si se omite, el sistema lo detecta automáticamente según el
            nombre.
        max_capacity:
          type: integer
          minimum: 1
          nullable: true
          description: Capacidad máxima de productos. Recomendado para vitrinas.
        storage_type_id:
          type: string
          format: uuid
          nullable: true
        storage_template_id:
          type: string
          format: uuid
          nullable: true
        operational_status:
          allOf:
          - $ref: '#/components/schemas/OperationalStatusEnum'
          default: active
        capacity_mode:
          allOf:
          - $ref: '#/components/schemas/CapacityModeEnum'
          default: none
        capacity_level:
          type: integer
          maximum: 5
          minimum: 1
          nullable: true
        capacity_score:
          type: integer
          minimum: 1
          nullable: true
        occupancy_estimate_pct:
          type: number
          format: double
          maximum: 100.0
          minimum: 0.0
          nullable: true
      required:
      - name
    LocationRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 100
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: string
        is_retail:
          type: boolean
          description: True si la ubicación es un punto de venta minorista (vitrina,
            mostrador, etc.).
        max_capacity:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
          description: Capacidad máxima de productos. Aplica principalmente a vitrinas.
        storage_type_id:
          type: string
          format: uuid
          nullable: true
        storage_template_id:
          type: string
          format: uuid
          nullable: true
        operational_status:
          allOf:
          - $ref: '#/components/schemas/OperationalStatusEnum'
          description: |-
            Estado operativo de la ubicación para reglas de movimientos.

            * `active` - Activa
            * `maintenance` - Mantenimiento
            * `restricted` - Restringida
            * `blocked` - Bloqueada
            * `archived` - Archivada
        capacity_mode:
          allOf:
          - $ref: '#/components/schemas/CapacityModeEnum'
          description: |-
            Modo de capacidad para cálculo de ocupación informativa.

            * `none` - Sin capacidad
            * `relative_scale` - Escala relativa
            * `absolute_legacy` - Capacidad absoluta (legacy)
        capacity_level:
          type: integer
          maximum: 32767
          minimum: 0
          nullable: true
          description: Escala relativa (1-5) cuando capacity_mode=relative_scale.
        capacity_score:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
          description: Puntaje de capacidad relativo (unidad abstracta, no bloqueante).
        occupancy_estimate_pct:
          type: number
          format: double
          nullable: true
          description: Estimación informativa de ocupación calculada o ajustada.
        is_active:
          type: boolean
      required:
      - code
      - name
    LocationStateTransitionRequest:
      type: object
      properties:
        operational_status:
          $ref: '#/components/schemas/OperationalStatusEnum'
      required:
      - operational_status
    LoginRequestRequest:
      type: object
      description: 'Cuerpo de login: contraseña obligatoria; usuario o correo (al
        menos uno).'
      properties:
        username:
          type: string
          description: Nombre de usuario único.
        email:
          type: string
          format: email
          description: Correo registrado (alternativa a username).
        password:
          type: string
          writeOnly: true
          minLength: 1
      required:
      - password
    Movement:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        movement_type:
          allOf:
          - $ref: '#/components/schemas/MovementTypeEnum'
          readOnly: true
        product:
          type: string
          format: uuid
          readOnly: true
        product_sku:
          type: string
          readOnly: true
        lot:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        lot_code:
          type: string
          readOnly: true
        lot_expiration_date:
          type: string
          readOnly: true
        origin_location:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        destination_location:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        quantity:
          type: integer
          readOnly: true
        stock_previo_origen:
          type: integer
          readOnly: true
          nullable: true
        stock_resultante_origen:
          type: integer
          readOnly: true
          nullable: true
        stock_previo_destino:
          type: integer
          readOnly: true
          nullable: true
        stock_resultante_destino:
          type: integer
          readOnly: true
          nullable: true
        serial_number:
          type: string
          readOnly: true
          nullable: true
        quantity_invoiced:
          type: integer
          readOnly: true
          nullable: true
        discrepancy_note:
          type: string
          readOnly: true
          nullable: true
        justification:
          type: string
          readOnly: true
          nullable: true
        scanned_code:
          type: string
          readOnly: true
          nullable: true
        order_sku:
          type: string
          readOnly: true
          nullable: true
        invoice_number:
          type: string
          readOnly: true
          nullable: true
        invoice_pdf:
          type: string
          format: uri
          readOnly: true
          nullable: true
        related_movement:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        executed_by:
          type: string
          format: uuid
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        unit_price:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Precio unitario de venta congelado al momento del despacho.
        unit_cost:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Costo unitario congelado al momento del despacho.
        discount_pct:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          description: Porcentaje de descuento aplicado (0-100).
        discount_amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Monto de descuento calculado.
        subtotal:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: unit_price × quantity.
        tax_rate_pct:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          description: Tasa de IVA congelada al momento del despacho.
        tax_amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Monto de IVA calculado sobre la base imponible.
        total_amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: 'Total a cobrar: subtotal - discount_amount + tax_amount.'
        currency:
          type: string
          readOnly: true
          nullable: true
          description: Moneda ISO 4217 del despacho.
        price_type:
          type: string
          readOnly: true
          nullable: true
          description: 'Tipo de precio: retail | wholesale | cost | combo.'
        customer_snapshot:
          readOnly: true
          nullable: true
          description: Datos del cliente al momento del despacho (R-02).
      required:
      - created_at
      - currency
      - customer_snapshot
      - destination_location
      - discount_amount
      - discount_pct
      - discrepancy_note
      - executed_by
      - id
      - invoice_number
      - invoice_pdf
      - justification
      - lot
      - lot_code
      - lot_expiration_date
      - movement_type
      - order_sku
      - origin_location
      - price_type
      - product
      - product_sku
      - quantity
      - quantity_invoiced
      - related_movement
      - scanned_code
      - serial_number
      - stock_previo_destino
      - stock_previo_origen
      - stock_resultante_destino
      - stock_resultante_origen
      - subtotal
      - tax_amount
      - tax_rate_pct
      - total_amount
      - unit_cost
      - unit_price
    MovementReportPeriod:
      type: object
      properties:
        start:
          type: string
          format: date-time
        end:
          type: string
          format: date-time
      required:
      - end
      - start
    MovementReportProductItem:
      type: object
      properties:
        product__sku:
          type: string
        product__name:
          type: string
        total_qty:
          type: integer
      required:
      - product__name
      - product__sku
      - total_qty
    MovementReportResponse:
      type: object
      properties:
        counts:
          type: object
          additionalProperties:
            type: integer
        by_product:
          type: array
          items:
            $ref: '#/components/schemas/MovementReportProductItem'
        by_user:
          type: array
          items:
            $ref: '#/components/schemas/MovementReportUserItem'
        period:
          $ref: '#/components/schemas/MovementReportPeriod'
      required:
      - by_product
      - by_user
      - counts
      - period
    MovementReportUserItem:
      type: object
      properties:
        executed_by__username:
          type: string
        movements:
          type: integer
      required:
      - executed_by__username
      - movements
    MovementSummaryResponse:
      type: object
      properties:
        counts:
          type: object
          additionalProperties:
            type: integer
      required:
      - counts
    MovementTypeEnum:
      enum:
      - ENTRADA
      - SALIDA_VENTA_MAYOR
      - SALIDA_VENTA_MENOR
      - SALIDA_DANO
      - SALIDA_VENCIMIENTO
      - TRASLADO
      - AJUSTE
      - DEVOLUCION
      - SALIDA_COMBO
      type: string
      description: |-
        * `ENTRADA` - Entrada
        * `SALIDA_VENTA_MAYOR` - Salida venta mayor
        * `SALIDA_VENTA_MENOR` - Salida venta menor
        * `SALIDA_DANO` - Salida por daño
        * `SALIDA_VENCIMIENTO` - Salida por vencimiento
        * `TRASLADO` - Traslado interno
        * `AJUSTE` - Ajuste
        * `DEVOLUCION` - Devolución
        * `SALIDA_COMBO` - Salida por combo
    OperationalStatusEnum:
      enum:
      - active
      - maintenance
      - restricted
      - blocked
      - archived
      type: string
      description: |-
        * `active` - Activa
        * `maintenance` - Mantenimiento
        * `restricted` - Restringida
        * `blocked` - Bloqueada
        * `archived` - Archivada
    PaginatedAlertList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/Alert'
    PaginatedAuditLogList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/AuditLog'
    PaginatedCategoryList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/Category'
    PaginatedComboList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/Combo'
    PaginatedMovementList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/Movement'
    PaginatedProductList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/Product'
    PaginatedSubcategoryList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/Subcategory'
    PatchedCategoryUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
        requires_serial_number:
          type: boolean
        is_returnable:
          type: boolean
    PatchedComboUpdateRequest:
      type: object
      description: Campos editables de un combo. is_active no se acepta; usar DELETE/restore.
      properties:
        name:
          type: string
          minLength: 1
        sku:
          type: string
          minLength: 1
        items:
          type: array
          items:
            $ref: '#/components/schemas/ComboUpdateItemRequest'
        price_strategy:
          $ref: '#/components/schemas/PriceStrategyEnum'
        fixed_price_retail:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
        fixed_price_wholesale:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
    PatchedLocationRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 100
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: string
        is_retail:
          type: boolean
          description: True si la ubicación es un punto de venta minorista (vitrina,
            mostrador, etc.).
        max_capacity:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
          description: Capacidad máxima de productos. Aplica principalmente a vitrinas.
        storage_type_id:
          type: string
          format: uuid
          nullable: true
        storage_template_id:
          type: string
          format: uuid
          nullable: true
        operational_status:
          allOf:
          - $ref: '#/components/schemas/OperationalStatusEnum'
          description: |-
            Estado operativo de la ubicación para reglas de movimientos.

            * `active` - Activa
            * `maintenance` - Mantenimiento
            * `restricted` - Restringida
            * `blocked` - Bloqueada
            * `archived` - Archivada
        capacity_mode:
          allOf:
          - $ref: '#/components/schemas/CapacityModeEnum'
          description: |-
            Modo de capacidad para cálculo de ocupación informativa.

            * `none` - Sin capacidad
            * `relative_scale` - Escala relativa
            * `absolute_legacy` - Capacidad absoluta (legacy)
        capacity_level:
          type: integer
          maximum: 32767
          minimum: 0
          nullable: true
          description: Escala relativa (1-5) cuando capacity_mode=relative_scale.
        capacity_score:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
          description: Puntaje de capacidad relativo (unidad abstracta, no bloqueante).
        occupancy_estimate_pct:
          type: number
          format: double
          nullable: true
          description: Estimación informativa de ocupación calculada o ajustada.
        is_active:
          type: boolean
    PatchedProductPriceUpdateRequest:
      type: object
      description: Actualización parcial de precios de un producto (solo almacenista).
      properties:
        unit_cost:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
        sale_price_retail:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
        sale_price_wholesale:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
        tax_rate_pct:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          nullable: true
        currency:
          type: string
          minLength: 1
          maxLength: 3
    PatchedProductRequest:
      type: object
      properties:
        sku:
          type: string
          minLength: 1
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 255
        category:
          type: string
          format: uuid
        subcategory:
          type: string
          format: uuid
          nullable: true
        barcode:
          type: string
          nullable: true
          maxLength: 100
        brand:
          type: string
          minLength: 1
          maxLength: 100
        expiration_date:
          type: string
          format: date
          nullable: true
        requires_expiration:
          type: boolean
          description: True si el producto requiere control de vencimiento por lote.
        weight_grams:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
        requires_cold_chain:
          type: boolean
        is_active:
          type: boolean
        notes:
          type: string
        reorder_point:
          type: integer
          maximum: 2147483647
          minimum: 0
    PatchedStockThresholdRequest:
      type: object
      description: Actualización de umbral de stock por ubicación (NEW-02).
      properties:
        location_reorder_point:
          type: integer
          minimum: 0
          nullable: true
          description: Umbral local. null para eliminar el override y usar el umbral
            global del producto.
    PatchedStorageTemplateCreateRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          minLength: 1
        storage_type_id:
          type: string
          format: uuid
          nullable: true
        description:
          type: string
          default: ''
        defaults: {}
        is_system:
          type: boolean
          default: false
        sort_order:
          type: integer
          minimum: 0
          default: 0
    PatchedStorageTypeCreateRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          minLength: 1
        category:
          type: string
          default: general
        description:
          type: string
          default: ''
        capabilities: {}
        default_is_retail:
          type: boolean
          default: false
        is_system:
          type: boolean
          default: false
        sort_order:
          type: integer
          minimum: 0
          default: 0
    PatchedSubcategoryUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        category_id:
          type: string
          format: uuid
    PatchedUserRequest:
      type: object
      description: Perfil de usuario (RF-002) — lectura y actualización parcial por
        almacenista.
      properties:
        username:
          type: string
          minLength: 1
          title: Nombre de usuario
          description: 'Requerido. 150 carácteres como máximo. Únicamente letras,
            dígitos y @/./+/-/_ '
          pattern: ^[\w.@+-]+$
          maxLength: 150
        email:
          type: string
          format: email
          minLength: 1
          title: Correo electrónico
          maxLength: 254
        first_name:
          type: string
          title: Nombre
          maxLength: 150
        last_name:
          type: string
          title: Apellidos
          maxLength: 150
        phone:
          type: string
          maxLength: 20
        role:
          $ref: '#/components/schemas/RoleEnum'
        is_active:
          type: boolean
          title: Activo
          description: Indica si el usuario debe ser tratado como activo. Desmarque
            esta opción en lugar de borrar la cuenta.
    PatchedWebhookEndpointCreateRequest:
      type: object
      properties:
        url:
          type: string
          format: uri
          minLength: 1
          maxLength: 500
        secret:
          type: string
          minLength: 8
          maxLength: 128
        events:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 64
          description: 'Lista de event_types a suscribir. Ej: ["STOCK_CRITICO"].'
          minItems: 1
        is_active:
          type: boolean
          default: true
        max_retries:
          type: integer
          maximum: 10
          minimum: 1
          default: 3
    PerLocationStockRow:
      type: object
      properties:
        location_id:
          type: string
        location_code:
          type: string
        location_name:
          type: string
        storage_type_id:
          type: string
          nullable: true
        storage_type_code:
          type: string
          nullable: true
        storage_template_id:
          type: string
          nullable: true
        storage_template_code:
          type: string
          nullable: true
        operational_status:
          type: string
        capacity_mode:
          type: string
        capacity_level:
          type: integer
          nullable: true
        capacity_score:
          type: integer
          nullable: true
        occupancy_estimate_pct:
          type: number
          format: double
          nullable: true
        quantity:
          type: integer
      required:
      - location_code
      - quantity
    PerOrderSample:
      type: object
      properties:
        invoice_number:
          type: string
        movements:
          type: integer
        total_quantity:
          type: integer
        items:
          type: integer
        dispatched_at:
          type: string
          format: date-time
          nullable: true
      required:
      - dispatched_at
      - invoice_number
      - items
      - movements
      - total_quantity
    PrecisionEnum:
      enum:
      - real
      - partial
      - approximate
      - future
      type: string
      description: |-
        * `real` - real
        * `partial` - partial
        * `approximate` - approximate
        * `future` - future
    PriceStrategyEnum:
      enum:
      - derived
      - fixed
      type: string
      description: |-
        * `derived` - derived
        * `fixed` - fixed
    Product:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        sku:
          type: string
          maxLength: 100
        name:
          type: string
          maxLength: 255
        category:
          type: string
          format: uuid
        category_slug:
          type: string
          readOnly: true
        subcategory:
          type: string
          format: uuid
          nullable: true
        barcode:
          type: string
          nullable: true
          maxLength: 100
        barcode_type:
          type: string
          nullable: true
          readOnly: true
        brand:
          type: string
          maxLength: 100
        expiration_date:
          type: string
          format: date
          nullable: true
        requires_expiration:
          type: boolean
          description: True si el producto requiere control de vencimiento por lote.
        weight_grams:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
        requires_cold_chain:
          type: boolean
        is_active:
          type: boolean
        notes:
          type: string
        reorder_point:
          type: integer
          maximum: 2147483647
          minimum: 0
        unit_cost:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Costo de adquisición por unidad (COGS).
        sale_price_retail:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Precio de venta al por menor (vitrina).
        sale_price_wholesale:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Precio de venta al por mayor.
        tax_rate_pct:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          description: 'Tasa de IVA aplicable (ej: 19.00 para 19%).'
        currency:
          type: string
          readOnly: true
          description: Moneda ISO 4217 (COP por defecto).
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
      required:
      - barcode_type
      - category
      - category_slug
      - created_at
      - currency
      - id
      - name
      - sale_price_retail
      - sale_price_wholesale
      - sku
      - tax_rate_pct
      - unit_cost
      - updated_at
    ProductBarcode:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        sku:
          type: string
        name:
          type: string
        barcode:
          type: string
        barcode_type:
          type: string
        barcode_svg:
          type: string
        barcode_svg_data_uri:
          type: string
        render_format:
          type: string
      required:
      - barcode
      - barcode_svg
      - barcode_svg_data_uri
      - barcode_type
      - name
      - product_id
      - render_format
      - sku
    ProductCreateRequest:
      type: object
      properties:
        sku:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        category_id:
          type: string
          format: uuid
        subcategory_id:
          type: string
          format: uuid
          nullable: true
        barcode:
          type: string
          nullable: true
        brand:
          type: string
          minLength: 1
          default: Can
        requires_cold_chain:
          type: boolean
          default: false
        requires_expiration:
          type: boolean
          default: false
        expiration_date:
          type: string
          format: date
          nullable: true
        expiry_date:
          type: string
          format: date
          nullable: true
        weight_grams:
          type: integer
          minimum: 0
          nullable: true
        notes:
          type: string
          default: ''
        reorder_point:
          type: integer
          minimum: 0
          default: 0
        is_active:
          type: boolean
          default: true
      required:
      - category_id
      - name
      - sku
    ProductDetail:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        sku:
          type: string
          maxLength: 100
        name:
          type: string
          maxLength: 255
        category:
          type: string
          format: uuid
        category_slug:
          type: string
          readOnly: true
        subcategory:
          type: string
          format: uuid
          nullable: true
        barcode:
          type: string
          nullable: true
          maxLength: 100
        barcode_type:
          type: string
          nullable: true
          readOnly: true
        brand:
          type: string
          maxLength: 100
        expiration_date:
          type: string
          format: date
          nullable: true
        requires_expiration:
          type: boolean
          description: True si el producto requiere control de vencimiento por lote.
        weight_grams:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
        requires_cold_chain:
          type: boolean
        is_active:
          type: boolean
        notes:
          type: string
        reorder_point:
          type: integer
          maximum: 2147483647
          minimum: 0
        unit_cost:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Costo de adquisición por unidad (COGS).
        sale_price_retail:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Precio de venta al por menor (vitrina).
        sale_price_wholesale:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          readOnly: true
          nullable: true
          description: Precio de venta al por mayor.
        tax_rate_pct:
          type: string
          format: decimal
          pattern: ^-?\d{0,3}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          description: 'Tasa de IVA aplicable (ej: 19.00 para 19%).'
        currency:
          type: string
          readOnly: true
          description: Moneda ISO 4217 (COP por defecto).
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
        barcode_svg:
          type: string
          nullable: true
          readOnly: true
        barcode_svg_data_uri:
          type: string
          nullable: true
          readOnly: true
        barcode_payload:
          type: object
          additionalProperties:
            type: string
          nullable: true
          readOnly: true
      required:
      - barcode_payload
      - barcode_svg
      - barcode_svg_data_uri
      - barcode_type
      - category
      - category_slug
      - created_at
      - currency
      - id
      - name
      - sale_price_retail
      - sale_price_wholesale
      - sku
      - tax_rate_pct
      - unit_cost
      - updated_at
    ProductPriceHistory:
      type: object
      properties:
        id:
          type: string
          format: uuid
        field_changed:
          type: string
        old_value:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
        new_value:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,4})?$
          nullable: true
        currency:
          type: string
        changed_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
      required:
      - changed_by
      - created_at
      - currency
      - field_changed
      - id
      - new_value
      - old_value
    ProductRequest:
      type: object
      properties:
        sku:
          type: string
          minLength: 1
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 255
        category:
          type: string
          format: uuid
        subcategory:
          type: string
          format: uuid
          nullable: true
        barcode:
          type: string
          nullable: true
          maxLength: 100
        brand:
          type: string
          minLength: 1
          maxLength: 100
        expiration_date:
          type: string
          format: date
          nullable: true
        requires_expiration:
          type: boolean
          description: True si el producto requiere control de vencimiento por lote.
        weight_grams:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
        requires_cold_chain:
          type: boolean
        is_active:
          type: boolean
        notes:
          type: string
        reorder_point:
          type: integer
          maximum: 2147483647
          minimum: 0
      required:
      - category
      - name
      - sku
    QualityOperationalBreakdown:
      type: object
      properties:
        incident_units:
          type: integer
        damage_units:
          type: integer
        discard_units:
          type: integer
        return_units:
          type: integer
        quality_index_pct:
          type: number
          format: double
      required:
      - damage_units
      - discard_units
      - incident_units
      - quality_index_pct
      - return_units
    QualityOperationalItem:
      type: object
      properties:
        movement_type:
          type: string
        product_sku:
          type: string
        product_name:
          type: string
        movements:
          type: integer
        units:
          type: integer
      required:
      - movement_type
      - movements
      - product_name
      - product_sku
      - units
    QualityOperationalPeriod:
      type: object
      properties:
        days:
          type: integer
        start:
          type: string
          format: date-time
        end:
          type: string
          format: date-time
      required:
      - days
      - end
      - start
    QualityOperationalResponse:
      type: object
      properties:
        period:
          $ref: '#/components/schemas/QualityOperationalPeriod'
        totals:
          $ref: '#/components/schemas/QualityOperationalTotals'
        breakdown:
          $ref: '#/components/schemas/QualityOperationalBreakdown'
        by_type:
          type: array
          items:
            $ref: '#/components/schemas/QualityOperationalType'
        by_product:
          type: array
          items:
            $ref: '#/components/schemas/QualityOperationalItem'
        notes:
          type: array
          items:
            type: string
      required:
      - breakdown
      - by_product
      - by_type
      - notes
      - period
      - totals
    QualityOperationalTotals:
      type: object
      properties:
        movements:
          type: integer
        units:
          type: integer
      required:
      - movements
      - units
    QualityOperationalType:
      type: object
      properties:
        movement_type:
          type: string
        movements:
          type: integer
        units:
          type: integer
      required:
      - movement_type
      - movements
      - units
    ReportDataset:
      type: object
      properties:
        report:
          type: string
        generated_at:
          type: string
          format: date-time
        filters: {}
        data: {}
        suggested_filename:
          type: string
      required:
      - data
      - filters
      - generated_at
      - report
      - suggested_filename
    ReturnCreateRequest:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        location_id:
          type: string
          format: uuid
        quantity:
          type: integer
          minimum: 1
        lot_id:
          type: string
          format: uuid
          nullable: true
        serial_number:
          type: string
          nullable: true
        related_movement_id:
          type: string
          format: uuid
          nullable: true
      required:
      - location_id
      - product_id
      - quantity
    RoleEnum:
      enum:
      - almacenista
      - auxiliar_despacho
      - administrador
      type: string
      description: |-
        * `almacenista` - Almacenista
        * `auxiliar_despacho` - Auxiliar de despacho
        * `administrador` - Administrador
    SalesSummaryResponse:
      type: object
      properties:
        sales:
          $ref: '#/components/schemas/SalesTotals'
      required:
      - sales
    SalesTotals:
      type: object
      properties:
        mayor:
          type: integer
        menor:
          type: integer
      required:
      - mayor
      - menor
    SeverityEnum:
      enum:
      - CRITICAL
      - HIGH
      - MEDIUM
      - LOW
      - INFO
      type: string
      description: |-
        * `CRITICAL` - Crítico
        * `HIGH` - Alto
        * `MEDIUM` - Medio
        * `LOW` - Bajo
        * `INFO` - Informativo
    StatusEnum:
      enum:
      - PENDING
      - DELIVERED
      - FAILED
      type: string
      description: |-
        * `PENDING` - Pendiente
        * `DELIVERED` - Entregado
        * `FAILED` - Fallido
    StockByLocation:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        product:
          type: string
          format: uuid
          readOnly: true
        product_sku:
          type: string
          readOnly: true
        location:
          type: string
          format: uuid
          readOnly: true
        current_stock:
          type: integer
          readOnly: true
          description: 'BR-11: stock derivado; debe reconstruirse desde el ledger.'
        location_reorder_point:
          type: integer
          maximum: 2147483647
          minimum: 0
          nullable: true
          description: Override local de umbral de reorden. Si null, usa reorder_point
            del producto.
        effective_reorder_point:
          type: integer
          readOnly: true
        last_movement_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          description: Último movimiento que alteró este registro (auditoría rápida).
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
      required:
      - created_at
      - current_stock
      - effective_reorder_point
      - id
      - last_movement_at
      - location
      - product
      - product_sku
      - updated_at
    StockByProductResponse:
      type: object
      properties:
        product_id:
          type: string
        product_name:
          type: string
        sku:
          type: string
        by_location:
          type: array
          items:
            $ref: '#/components/schemas/PerLocationStockRow'
        per_location:
          type: array
          items:
            $ref: '#/components/schemas/PerLocationStockRow'
        total:
          type: integer
      required:
      - per_location
      - product_id
      - total
    StockReconstructRequestRequest:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        location_id:
          type: string
          format: uuid
      required:
      - location_id
      - product_id
    StockReconstructResponse:
      type: object
      properties:
        status:
          type: string
        reconstructed:
          type: integer
        actual:
          type: integer
      required:
      - actual
      - reconstructed
      - status
    StorageTemplate:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        code:
          type: string
          maxLength: 80
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          maxLength: 120
        storage_type_id:
          type: string
          format: uuid
          nullable: true
        storage_type_code:
          type: string
          readOnly: true
        storage_type_name:
          type: string
          readOnly: true
        description:
          type: string
        defaults: {}
        is_system:
          type: boolean
        is_active:
          type: boolean
        sort_order:
          type: integer
          maximum: 2147483647
          minimum: 0
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
      required:
      - code
      - created_at
      - id
      - name
      - storage_type_code
      - storage_type_name
      - updated_at
    StorageTemplateCreateRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          minLength: 1
        storage_type_id:
          type: string
          format: uuid
          nullable: true
        description:
          type: string
          default: ''
        defaults: {}
        is_system:
          type: boolean
          default: false
        sort_order:
          type: integer
          minimum: 0
          default: 0
      required:
      - code
      - name
    StorageType:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        code:
          type: string
          maxLength: 80
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          maxLength: 100
        category:
          type: string
          maxLength: 50
        description:
          type: string
        capabilities: {}
        default_is_retail:
          type: boolean
        is_system:
          type: boolean
        is_active:
          type: boolean
        sort_order:
          type: integer
          maximum: 2147483647
          minimum: 0
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
      required:
      - code
      - created_at
      - id
      - name
      - updated_at
    StorageTypeCreateRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          minLength: 1
        category:
          type: string
          default: general
        description:
          type: string
          default: ''
        capabilities: {}
        default_is_retail:
          type: boolean
          default: false
        is_system:
          type: boolean
          default: false
        sort_order:
          type: integer
          minimum: 0
          default: 0
      required:
      - code
      - name
    Subcategory:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        category:
          type: string
          format: uuid
        name:
          type: string
          maxLength: 128
        slug:
          type: string
          maxLength: 128
          pattern: ^[-a-zA-Z0-9_]+$
        is_active:
          type: boolean
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
      required:
      - category
      - created_at
      - id
      - name
      - slug
      - updated_at
    SubcategoryCreateRequest:
      type: object
      properties:
        category_id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 1
      required:
      - category_id
      - name
    SubcategoryUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        category_id:
          type: string
          format: uuid
    TopDispatchedProduct:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        sku:
          type: string
        name:
          type: string
        dispatched_quantity:
          type: integer
      required:
      - dispatched_quantity
      - name
      - product_id
      - sku
    TransferCreateRequest:
      type: object
      properties:
        product_id:
          type: string
          format: uuid
        origin_id:
          type: string
          format: uuid
        destination_id:
          type: string
          format: uuid
        quantity:
          type: integer
          minimum: 1
        lot_id:
          type: string
          format: uuid
          nullable: true
        cold_chain_acknowledged:
          type: boolean
          default: false
        electrical_safety_acknowledged:
          type: boolean
          default: false
      required:
      - destination_id
      - origin_id
      - product_id
      - quantity
    User:
      type: object
      description: Perfil de usuario (RF-002) — lectura y actualización parcial por
        almacenista.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        username:
          type: string
          title: Nombre de usuario
          description: 'Requerido. 150 carácteres como máximo. Únicamente letras,
            dígitos y @/./+/-/_ '
          pattern: ^[\w.@+-]+$
          maxLength: 150
        email:
          type: string
          format: email
          title: Correo electrónico
          maxLength: 254
        first_name:
          type: string
          title: Nombre
          maxLength: 150
        last_name:
          type: string
          title: Apellidos
          maxLength: 150
        phone:
          type: string
          maxLength: 20
        role:
          $ref: '#/components/schemas/RoleEnum'
        is_active:
          type: boolean
          title: Activo
          description: Indica si el usuario debe ser tratado como activo. Desmarque
            esta opción en lugar de borrar la cuenta.
        is_staff:
          type: boolean
          readOnly: true
          title: Es staff
          description: Indica si el usuario puede entrar en este sitio de administración.
        created_by:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
        last_login:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          title: Último inicio de sesión
      required:
      - created_at
      - created_by
      - email
      - id
      - is_staff
      - last_login
      - role
      - updated_at
      - username
    UserCreateRequest:
      type: object
      properties:
        username:
          type: string
          minLength: 1
          maxLength: 150
        password:
          type: string
          writeOnly: true
          minLength: 8
        email:
          type: string
          format: email
          minLength: 1
        first_name:
          type: string
          default: ''
        last_name:
          type: string
          default: ''
        phone:
          type: string
          default: ''
          maxLength: 20
        role:
          $ref: '#/components/schemas/RoleEnum'
      required:
      - email
      - password
      - role
      - username
    UserRequest:
      type: object
      description: Perfil de usuario (RF-002) — lectura y actualización parcial por
        almacenista.
      properties:
        username:
          type: string
          minLength: 1
          title: Nombre de usuario
          description: 'Requerido. 150 carácteres como máximo. Únicamente letras,
            dígitos y @/./+/-/_ '
          pattern: ^[\w.@+-]+$
          maxLength: 150
        email:
          type: string
          format: email
          minLength: 1
          title: Correo electrónico
          maxLength: 254
        first_name:
          type: string
          title: Nombre
          maxLength: 150
        last_name:
          type: string
          title: Apellidos
          maxLength: 150
        phone:
          type: string
          maxLength: 20
        role:
          $ref: '#/components/schemas/RoleEnum'
        is_active:
          type: boolean
          title: Activo
          description: Indica si el usuario debe ser tratado como activo. Desmarque
            esta opción en lugar de borrar la cuenta.
      required:
      - email
      - role
      - username
    WarehouseUtilizationLocation:
      type: object
      properties:
        location_id:
          type: string
          format: uuid
        code:
          type: string
        name:
          type: string
        occupied_units:
          type: integer
        capacity_units:
          type: integer
          nullable: true
        utilization_pct:
          type: number
          format: double
          nullable: true
        is_retail:
          type: boolean
        capacity_configured:
          type: boolean
        capacity_mode:
          type: string
        capacity_level:
          type: integer
          nullable: true
        capacity_score:
          type: integer
          nullable: true
        occupancy_estimate_pct:
          type: number
          format: double
          nullable: true
        operational_status:
          type: string
        storage_type_code:
          type: string
          nullable: true
        storage_type_name:
          type: string
          nullable: true
      required:
      - capacity_configured
      - capacity_level
      - capacity_mode
      - capacity_score
      - capacity_units
      - code
      - is_retail
      - location_id
      - name
      - occupancy_estimate_pct
      - occupied_units
      - operational_status
      - storage_type_code
      - storage_type_name
      - utilization_pct
    WarehouseUtilizationOperationalStatus:
      type: object
      properties:
        operational_status:
          type: string
        locations:
          type: integer
        occupied_units:
          type: integer
      required:
      - locations
      - occupied_units
      - operational_status
    WarehouseUtilizationResponse:
      type: object
      properties:
        overall:
          $ref: '#/components/schemas/WarehouseUtilizationSummary'
        by_location:
          type: array
          items:
            $ref: '#/components/schemas/WarehouseUtilizationLocation'
        by_storage_type:
          type: array
          items:
            $ref: '#/components/schemas/WarehouseUtilizationStorageType'
        by_operational_status:
          type: array
          items:
            $ref: '#/components/schemas/WarehouseUtilizationOperationalStatus'
      required:
      - by_location
      - by_operational_status
      - by_storage_type
      - overall
    WarehouseUtilizationStorageType:
      type: object
      properties:
        storage_type_code:
          type: string
        storage_type_name:
          type: string
        locations:
          type: integer
        occupied_units:
          type: integer
      required:
      - locations
      - occupied_units
      - storage_type_code
      - storage_type_name
    WarehouseUtilizationSummary:
      type: object
      properties:
        occupied_units:
          type: integer
        capacity_units:
          type: integer
        utilization_pct:
          type: number
          format: double
          nullable: true
        configured_locations:
          type: integer
        locations_without_capacity:
          type: integer
        unconfigured_occupied_units:
          type: integer
      required:
      - capacity_units
      - configured_locations
      - locations_without_capacity
      - occupied_units
      - unconfigured_occupied_units
      - utilization_pct
    WebhookDelivery:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        endpoint:
          type: string
          format: uuid
          readOnly: true
        endpoint_url:
          type: string
          readOnly: true
        event_type:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/StatusEnum'
          readOnly: true
        attempts:
          type: integer
          readOnly: true
        last_attempt_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        next_retry_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        response_code:
          type: integer
          readOnly: true
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
      required:
      - attempts
      - created_at
      - endpoint
      - endpoint_url
      - event_type
      - id
      - last_attempt_at
      - next_retry_at
      - response_code
      - status
    WebhookEndpoint:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        url:
          type: string
          format: uri
          maxLength: 500
        events:
          description: 'Lista de event_types suscritos. Ej: ["STOCK_CRITICO"].'
        is_active:
          type: boolean
        max_retries:
          type: integer
          maximum: 32767
          minimum: 0
        created_by:
          type: string
          format: uuid
          readOnly: true
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de creación (UTC).
        updated_at:
          type: string
          format: date-time
          readOnly: true
          description: Fecha y hora de última modificación (UTC).
      required:
      - created_at
      - created_by
      - id
      - updated_at
      - url
    WebhookEndpointCreateRequest:
      type: object
      properties:
        url:
          type: string
          format: uri
          minLength: 1
          maxLength: 500
        secret:
          type: string
          minLength: 8
          maxLength: 128
        events:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 64
          description: 'Lista de event_types a suscribir. Ej: ["STOCK_CRITICO"].'
          minItems: 1
        is_active:
          type: boolean
          default: true
        max_retries:
          type: integer
          maximum: 10
          minimum: 1
          default: 3
      required:
      - events
      - secret
      - url
    WebhookStatsResponse:
      type: object
      properties:
        pending:
          type: integer
        delivered:
          type: integer
        failed:
          type: integer
        active_endpoints:
          type: integer
      required:
      - active_endpoints
      - delivered
      - failed
      - pending
    WebhookTestRequest:
      type: object
      properties:
        event_type:
          type: string
          minLength: 1
          default: TEST
          maxLength: 64
        payload:
          type: object
          additionalProperties: {}
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: 'Prefijo literal: Bearer + espacio + access_token (login o refresh).'
    jwtAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
tags:
- name: auth
  description: JWT authentication and user management.
- name: system
  description: System availability checks.
- name: catalog
  description: Categories, products, combos and SKU management.
- name: inventory
  description: Locations and stock tracking.
- name: movements
  description: 'Ledger: entries, dispatches, transfers, returns, adjustments and invoices.'
- name: dashboard
  description: Operational read model for executive UI and KPIs.
- name: reports
  description: Read-only reports and KPIs.
- name: alerts
  description: Operational alerts and real-time polling.
- name: audit
  description: Audit logs (read-only, immutable).
- name: webhooks
  description: Webhook endpoints and delivery management (almacenista only — IsAlmacenista).
